#spring:
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://localhost:5432/sqlTest
#    username: postgres
#    password: postgres

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}  # 環境変数がなければ local
